// Composables index - Just type exports for complex imports
// Note: Nuxt 3 auto-imports composables automatically from individual files
// This file should only be used for type exports to avoid duplication warnings

// Re-export types for convenience in type definitions
export type * from '~/types'

// Note: All composables (useApi, useAuth, etc.) and stores (useAuthStore, etc.) 
// are auto-imported by Nuxt and available globally without explicit imports
